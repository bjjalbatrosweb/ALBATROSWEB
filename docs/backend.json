
{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Stores personal and biometric data for an athlete, enabling nutrition and training calculations.",
      "properties": {
        "id": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "firstName": { "type": "string" },
        "lastName": { "type": "string" },
        "age": { "type": "number" },
        "gender": { "type": "string" },
        "heightCm": { "type": "number" },
        "weightKg": { "type": "number" },
        "activityLevel": { "type": "number" },
        "athleticDiscipline": { "type": "string" },
        "goal": { "type": "string", "enum": ["lose", "maintain", "gain"] },
        "dailyTargetCalories": { "type": "number" },
        "dailyTargetProtein": { "type": "number" },
        "dailyTargetFats": { "type": "number" },
        "dailyTargetCarbs": { "type": "number" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "email", "firstName", "lastName", "createdAt", "updatedAt"]
    },
    "AdminAlumno": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AdminAlumno",
      "type": "object",
      "description": "Datos administrativos de un alumno del equipo Albatros BJJ.",
      "properties": {
        "id": { "type": "string" },
        "rfid": { "type": "string", "description": "Código de tarjeta RFID vinculada." },
        "nombre": { "type": "string" },
        "telefono": { "type": "string" },
        "diaPago": { "type": "number" },
        "esAfiliado": { "type": "boolean" },
        "descuento": { "type": "number" },
        "montoPago": { "type": "number" },
        "estadoPago": { "type": "string", "enum": ["Pagado", "Falta de Pago", "Retraso"] },
        "fechaRegistro": { "type": "string", "format": "date-time" }
      },
      "required": ["nombre", "diaPago", "montoPago"]
    },
    "Asistencia": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Asistencia",
      "type": "object",
      "description": "Registro de entrada de un alumno al tatami.",
      "properties": {
        "alumnoId": { "type": "string" },
        "nombre": { "type": "string" },
        "rfid": { "type": "string" },
        "dispositivo": { "type": "string" },
        "fecha": { "type": "string", "format": "date-time" },
        "acceso": { "type": "string" }
      }
    },
    "VinculacionRFID": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "VinculacionRFID",
      "type": "object",
      "description": "Registro de proceso de vinculación de tarjeta física a un alumno.",
      "properties": {
        "id": { "type": "string" },
        "alumnoId": { "type": "string" },
        "estado": { "type": "string", "enum": ["pendiente", "completada", "cancelada"] },
        "dispositivo": { "type": "string" },
        "rfidAsignado": { "type": "string" },
        "creadoEn": { "type": "string", "format": "date-time" },
        "completadoEn": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": ["password", "anonymous"]
  },
  "firestore": {
    "/perfiles/{userId}": {
      "schema": "UserProfile",
      "description": "Perfiles tácticos de los atletas."
    },
    "/Alumnos/{alumnoId}": {
      "schema": "AdminAlumno",
      "description": "Colección central de gestión de alumnos del equipo."
    },
    "/Asistencias/{asistenciaId}": {
      "schema": "Asistencia",
      "description": "Log de accesos y asistencias diarias."
    },
    "/VinculacionesRFID/{vinculacionId}": {
      "schema": "VinculacionRFID",
      "description": "Procesos de emparejamiento de hardware y alumno."
    }
  }
}
