import { GET as consultarCompras, POST as crearCompra } from '../route (1)';

export const runtime = 'nodejs';
export const dynamic = 'force-dynamic';

export const GET = consultarCompras;
export const POST = crearCompra;
