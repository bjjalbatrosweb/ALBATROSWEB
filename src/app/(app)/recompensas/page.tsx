// Este archivo ha sido desactivado permanentemente para evitar conflictos de rutas paralelas
// con la página principal ubicada en src/app/recompensas/page.tsx.
// Al no exportar un componente por defecto, Next.js no lo procesará como una ruta.

export default function DeadRoute() {
  return null;
}
