/**
 * ARCHIVO DESACTIVADO
 * Este archivo ha sido desactivado para evitar conflictos de rutas con /src/app/recompensas/page.tsx.
 * La página de recompensas ahora es independiente y pública en la raíz del proyecto.
 * No debe contener un 'export default' para que Next.js no lo reconozca como una página válida.
 */
export const metadata = {
  robots: {
    index: false,
    follow: false,
  },
};

const InactivePage = () => null;
// No exportamos por defecto para eliminar la ruta paralela
