"use client";
export type ProgressAthlete = { id: string; nombre: string; disciplina: string; habilidades?: Record<string, "pendiente" | "practicando" | "dominada">; historialFisico?: unknown[] };
export function AdminAthletePicker({ athletes, selectedId, onSelect }: { athletes: ProgressAthlete[]; selectedId: string; onSelect: (id: string) => void }) { return <label className="block"><span className="mb-2 block text-xs font-black uppercase tracking-wider text-slate-400">Atleta</span><select value={selectedId} onChange={(e) => onSelect(e.target.value)} className="w-full rounded-xl border border-white/15 bg-slate-950 p-3 font-bold text-white"><option value="">Selecciona un atleta</option>{athletes.map((a) => <option key={a.id} value={a.id}>{a.nombre} · {a.disciplina || "Sin disciplina"}</option>)}</select></label>; }
